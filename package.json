{
  "name": "gql-syntax-master-thesis",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "setup": "node scripts/setup.js",
    "parser:build": "yarn parser:generate && yarn workspace gql-parser build",
    "parser:generate": "yarn workspace gql-parser generate",
    "interface:dev": "yarn workspace gql-editor start",
    "interface:deploy": "yarn workspace gql-editor build && yarn workspace gql-editor deploy"
  },
  "workspaces": [
    "workspaces/editor",
    "workspaces/parser"
  ]
}
